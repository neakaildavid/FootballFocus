# Environment files (never commit real secrets/connection strings)
.env
.env.local
.env.*.local

# Python (pipeline/, once built)
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# web/ has its own .gitignore for node_modules/.next/etc.
